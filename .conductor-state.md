# Conductor state

task: (none)
started: (none)
status: map-done
framework: Next.js 16 (App Router) + TypeScript 5 + Tailwind CSS 3.4 + Framer Motion 11

## Section map
(mirror of .conductor/section-map.json)

- data: none
- logic: none
- routes: `app/layout.tsx`, `app/page.tsx`
- interface: `app/globals.css`, `components/**`, `styles/**`, `public/**`
- tests: none
- config: `package.json`, `package-lock.json`, `next.config.mjs`, `postcss.config.mjs`, `tailwind.config.ts`, `tsconfig.json`, `next-env.d.ts`
- docs: `README.md`

Notes:
- Next.js App Router mixes sections in app/: `app/page.tsx` and `app/layout.tsx` define the route tree (`/` only, no `app/api/**`), so they are assigned to routes; their rendered content and `app/globals.css` are covered by interface globs.
- Static portfolio with no models, schemas, ORM, or local storage — data is `none`.
- No services, state management, or utils beyond inline component logic — logic is `none`.
- No test files or test config anywhere in the tree — tests is `none`.
- `public/images` is currently empty; kept under interface (static assets).

## Contracts
(written by orchestrator in `plan`. One block per contract. Agents read only the
contracts named in their work order. Format:)

### C-001 <name>
owner: <section that implements it>
consumers: <sections that depend on it>
spec:
  (types / signatures / endpoint shape / component props — exact, not prose)

## Work orders
(one per section with work to do this run. Sections with no work order are skipped.)

### data [status: pending]
contracts: C-001
do:
  -
done-when:
  -

## Summaries
(each section agent appends when finished)

## Deviations
(any agent that cannot honor a contract logs it here and STOPS — it does not improvise)

## Verify
tests:
review:

## Report
